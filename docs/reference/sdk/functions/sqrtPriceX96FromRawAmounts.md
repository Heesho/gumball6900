[**GUM BALL 6900 TypeScript SDK**](../README.md)

---

[GUM BALL 6900 TypeScript SDK](../README.md) / sqrtPriceX96FromRawAmounts

# Function: sqrtPriceX96FromRawAmounts()

> **sqrtPriceX96FromRawAmounts**(`amount0`, `amount1`): `bigint`

Uses Uniswap's canonical integer encodeSqrtRatioX96 implementation; amount1/amount0 is a raw-unit ratio.

## Parameters

| Parameter | Type     |
| --------- | -------- |
| `amount0` | `bigint` |
| `amount1` | `bigint` |

## Returns

`bigint`
