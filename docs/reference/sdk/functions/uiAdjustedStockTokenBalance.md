[**GUM BALL 6900 TypeScript SDK**](../README.md)

---

[GUM BALL 6900 TypeScript SDK](../README.md) / uiAdjustedStockTokenBalance

# Function: uiAdjustedStockTokenBalance()

> **uiAdjustedStockTokenBalance**(`rawBalance`, `uiMultiplier`): `bigint`

Applies ERC-8056's display multiplier without changing raw custody or redemption accounting.

## Parameters

| Parameter      | Type     |
| -------------- | -------- |
| `rawBalance`   | `bigint` |
| `uiMultiplier` | `bigint` |

## Returns

`bigint`
